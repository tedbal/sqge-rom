import matplotlib.pyplot as plt
import numpy as np

import build_fe_operators
import build_rom_basis_and_operators
import build_sindy_model
import plot_and_compare

# define constants for the problem

# build the finite element operators

# plot the finite element solution

# build the ROM operators

# define the SINDy dictionary of search functions

# build the SINDy model

# define some new regime

# get the ground truth of the new regime

# get the ROM and SINDy evaluation of the new regime

# compute errors

# plot
